export const Roles = {
    user:'user',
    admin:'admin'
}
export const Providers = {
google:'google',
system:'system'
}
export const tokenTypes = {
    access:'access',
    refresh:'refresh'
}
Object.freeze(Providers);
Object.freeze(Roles);
Object.freeze(tokenTypes);